# Python side of HEPHAESTUS: artifact export + golden-test oracle.
# The C++ engine has ZERO third-party dependencies by design.

numpy>=1.26,<2.1
pyyaml>=6,<7
pytest>=8,<9
torch>=2.3,<2.5          # only used by scripts/export_artifacts.py
tokenizers>=0.19,<0.21   # cross-check of the C++ BPE against HF
